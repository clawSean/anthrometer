{
  "name": "anthrometer",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "description": "No-LLM Anthropic usage meter plugin for OpenClaw",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ],
    "runtimeExtensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
